# Veneur, in Docker

This repository provides a Dockerfile and config.yaml for running [Veneur](https://github.com/stripe/veneur) for Meetup's classic environment. Veneur is a server implementation of the DogStatsD protocol for aggregating metrics and sending them to downstream storage.
